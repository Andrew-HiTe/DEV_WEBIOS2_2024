let n1 = prompt("Digite um número")
let n2 = prompt("Digite outro número")

if (n1 > n2) {
    console.log(`O primeiro número (${n1}) é maior que o número 2 (${n2})`);
}
else {
    console.log(`O primeiro número (${n1}) é menor que o número 2 (${n2})`)
}